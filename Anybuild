# Generated by Anybuild — https://anybuild.run
# Custom config for Wasmer Git deploy.

load("//anybuild/tools:node.bzl", "node_build", "node_config", "node_serve")

config = node_config(
    schema = 1,
    commands = {
        "start": "npm start",
    },
    node_package_manager = "npm",
    node_server = "node",
    node_build_command = "npm run build",
    node_version = "24",
    node_install_requires_all_files = True,
)

build = node_build(config)

node_serve(config, build, name = "ims-ssl")
